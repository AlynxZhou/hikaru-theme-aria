{% if page.layout === "index" or page.title === "index" %}
{% include "includes/index.njk" %}
{% elif page.layout === "archive" or page.title === "archive" %}
{% include "includes/archive.njk" %}
{% elif page.layout === "categories" or page.title === "categories" %}
{% include "includes/categories.njk" %}
{% elif page.layout === "category" or page.title === "category" %}
{% include "includes/category.njk" %}
{% elif page.layout === "tags" or page.title === "tags" %}
{% include "includes/tags.njk" %}
{% elif page.layout === "tag" or page.title === "tag" %}
{% include "includes/tag.njk" %}
{% elif page.layout === "search" or page.title === "search" %}
{% include "includes/search.njk" %}
{% else %}
{% extends "layout.njk" %}

{% block css %}
<link rel="stylesheet" type="text/css" href="{{ getPath("css/page.css") }}">
<link rel="stylesheet" type="text/css" href="{{ getPath("css/post.css") }}">
{% endblock %}

{% block title %}{{ __(title) }} | {{ siteConfig.title }}{% if siteConfig.subtitle %} - {{ siteConfig.subtitle }}{% endif %}{% endblock %}

{% block content %}
<div id="page" class="page">
  <article class="article post card{% if themeConfig.animate %} animate{% endif %}">
    <div class="post-block">
      <link itemprop="mainEntityOfPage" href="{{ getURL(docPath) }}">
      <span hidden itemprop="author" itemscope itemtype="http://schema.org/Person">
        <meta itemprop="name" content="{{ siteConfig.author }}">
        <meta itemprop="description" content="{{ siteConfig.description }}">
        <meta itemprop="image" content="{{ getPath(themeConfig.avatar) }}">
      </span>
      <span hidden itemprop="publisher" itemscope itemtype="http://schema.org/Organization">
        <meta itemprop="name" content="{{ siteConfig.title }}">
      </span>
    </div>
    <header class="post-header">
      <h1 class="post-title" itemprop="name headline">{{ __(title) }}</h1>
      <div class="post-meta">
        {% if createdDate %}
        <span class="post-date meta-block">
          <i class="far fa-calendar-plus"></i><span><time title="post-date" itemprop="dateCreated datePublished" datetime="{{ createdDate.toISOString() }}">{{ createdMoment.format("YYYY-MM-DD ddd HH:mm:ss z") }}</time></span>
        </span>
        {% endif %}
        {% if comment !== false and themeConfig.comment.enable %}
        {% if themeConfig.comment.issue.enable %}
        <span class="post-comment-count meta-block">
          <i class="far fa-comments"></i><span><a href="{{ getPath(docPath) }}#comment-results" itemprop="discussionUrl"><span class="post-comment-count comment-count" data-comment-identifier="{{ __(title) }}" itemprop="commentCount">{{ __("comment.read") }}</span></a></span>
        </span>
        {% elif themeConfig.comment.disqusShortName %}
        <span class="post-comment-count meta-block">
          <i class="far fa-comments"></i><span><a href="{{ getPath(docPath) }}#disqus_thread" itemprop="discussionUrl"><span class="post-comment-count disqus-comment-count" data-disqus-identifier="{{ getPath(docPath) }}" itemprop="commentCount"></span></a></span>
        </span>
        {% endif %}
        {% endif %}
      </div>
    </header>
    <main class="post-main" id="scrollspy-container">
      {{ content }}
    </main>
    <footer class="post-footer"></footer>
  </article>
  {% if themeConfig.reward.enable %}
  {% include "includes/reward.njk" %}
  {% endif %}
  {% if themeConfig.comment.enable %}
  {% include "includes/comment.njk" %}
  {% endif %}
</div>
{% endblock %}
{% endif %}
